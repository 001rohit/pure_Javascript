// multiplication table 

const multi_table = (a) =>{
    for(let i = 1 ; i<=10;i++){
      console.log(`${a} * ${i} = ${a *i}`);
    }
}
multi_table(5);

// const multiplication_table = (a) =>{
//       console.log(`Multiplication Table for ${a}`);
//       for(let i = 1 ; i<=10 ; i++){
//         console.log(`${a} * ${i} = ${a * i }`)
//       }
// }
// multiplication_table(120)